
describe('query message', () => {
  it('should return an error object if the data is not an object', (done) => {
  });

  it('should return an error object if data.type is invalid', (done) => {
  });

});
